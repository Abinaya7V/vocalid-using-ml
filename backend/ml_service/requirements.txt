# VocalID ML Service v2 - Multi-Speaker Recognition
# Install: pip install -r requirements.txt

# ── FastAPI server ────────────────────────────────────────────
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.6

# ── Speaker embeddings (Resemblyzer) ─────────────────────────
resemblyzer>=0.1.1.dev0
torch>=2.0.0           # CPU version is fine; GPU optional
numpy>=1.24.0
scipy>=1.10.0

# ── Audio processing ──────────────────────────────────────────
librosa>=0.10.0
soundfile>=0.12.1
audioread>=3.0.0       # WebM/OGG decode via ffmpeg
audioread>=3.0.0

# Database
pymongo>=4.6.0
python-dotenv>=1.0.0

# Speaker Recognition (PyAnnote handles everything now)
pyannote.audio>=3.1.0
huggingface_hub>=0.17.0
speechbrain>=1.0.0
