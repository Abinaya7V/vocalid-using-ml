# VocalID ML Service — Voice + Face + Lip-Sync Dependencies
# Install with:  pip install -r requirements.txt   (inside your .venv)

# Face detection/recognition (YuNet + SFace) — MUST be the contrib build;
# plain opencv-python does NOT include FaceDetectorYN / FaceRecognizerSF.
opencv-contrib-python>=4.8.0

numpy>=1.24.0
scipy>=1.10.0

# Voice biometrics (used by utils.py, predict_speaker.py, train_model.py)
librosa>=0.10.0
scikit-learn>=1.3.0
joblib>=1.3.0
soundfile>=0.12.1     # backend librosa uses for WAV/FLAC
audioread>=3.0.0       # fallback decoder for mp3/webm/mp4 (needs ffmpeg on PATH)
